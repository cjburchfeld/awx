---
- hosts: all
  become: true
  tasks:

  - ansible.builtin.apt:
      upgrade: yes
      update_cache: yes
    when: ansible_facts['os_family'] == "Debian"

  - ansible.builtin.dnf:
      name: "*"
      state: latest
      update_cache: true
    when: ansible_facts['os_family'] == "RedHat"
